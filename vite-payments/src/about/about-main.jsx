import React from "react";
import ReactDOM from "react-dom/client";
import About from "./About"; // Your actual page content

ReactDOM.createRoot(document.getElementById("about-root")).render(
  <React.StrictMode>
    <About />
  </React.StrictMode>,
);
